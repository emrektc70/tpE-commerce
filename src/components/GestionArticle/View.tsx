import { Button } from "@material-ui/core";
import styles from "./styles.module.scss";

type Props = {
};

const View: React.FC<Props> = () => {
  return (
    <div>
      test
    </div>

  )
};

export default View;
